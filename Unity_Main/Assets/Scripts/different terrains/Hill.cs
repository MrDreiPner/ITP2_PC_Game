using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hill : TerrainTypes
{
    public Hill()
    {
        moveMod = 0;
        atkModUnit = Types.range;
        atkMod = 0.2f;
        defMod = 0.2f;
    }
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
