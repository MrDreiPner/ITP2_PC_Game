using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Forest : TerrainTypes
{
    public Forest()
    {
        moveMod = -1;
        atkMod = 0;
        defModUnit = Types.infantry;
        defMod = 0.2f;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
