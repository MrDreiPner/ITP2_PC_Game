using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Impasse : TerrainTypes
{
    const bool moveCheck = false;

    public Impasse()
    {
        moveMod = 0;
        atkMod = 0;
        defMod = 0;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
